# TaskMini — Дизайн-документ

> Простой помощник для церковных и волонтёрских команд, чтобы не забывать договорённости

## Обзор

**Проблема:** В церковных и волонтёрских командах задачи раздаются в чате, голосом, на собраниях — и теряются. Неясно кто отвечает, нет сроков, лидер всё держит в голове.

**Решение:** TaskMini — Mini App в Telegram для простого управления задачами команды. Не "менеджмент", а помощник в служении.

**Технологии:** Google Apps Script + Google Sheets + Telegram Mini App SDK

---

## Принятые решения

| Аспект | Решение |
|--------|---------|
| Архитектура | Mini App на Google Apps Script |
| Кто создаёт задачи | Настраивается лидером команды (все / только лидер) |
| Тон бота | Нейтральный |
| Напоминания | Настраиваемые пользователем |
| Команды | Пользователь может состоять в нескольких |
| Назначение задач | Себе и другим участникам команды |
| Видимость | Все видят все задачи команды |
| Срок задачи | Опциональный |
| Статусы | Открыта → В работе → Выполнена |
| Редактирование | Любой участник команды |

---

## Секция 1: Модель данных

### Google Sheets структура

#### Sheet 1: `teams`

| Поле | Тип | Описание |
|------|-----|----------|
| team_id | string | UUID команды |
| name | string | Название команды |
| created_by | string | Telegram ID создателя |
| created_at | datetime | Дата создания |
| task_creation_mode | string | "all" или "leader_only" |
| invite_code | string | Код для приглашения |

#### Sheet 2: `team_members`

| Поле | Тип | Описание |
|------|-----|----------|
| team_id | string | FK на teams |
| user_id | string | Telegram ID |
| username | string | @username |
| display_name | string | Имя в Telegram |
| role | string | "leader" или "member" |
| joined_at | datetime | Дата вступления |

#### Sheet 3: `tasks`

| Поле | Тип | Описание |
|------|-----|----------|
| task_id | string | UUID задачи |
| team_id | string | FK на teams |
| title | string | Описание задачи |
| assignee_id | string | Telegram ID исполнителя |
| created_by | string | Telegram ID создателя |
| status | string | "open" / "in_progress" / "done" |
| due_date | datetime | Срок (опционально) |
| reminder_settings | json | Настройки напоминаний |
| created_at | datetime | Создана |
| updated_at | datetime | Обновлена |

---

## Секция 2: Экраны Mini App

### Основная навигация

```
┌─────────────────────────────────┐
│  [Команда ▼]     TaskMini       │  ← Переключатель команд
├─────────────────────────────────┤
│                                 │
│         Контент экрана          │
│                                 │
├─────────────────────────────────┤
│  [Мои]   [Все]   [+ Задача]     │  ← Нижняя навигация
└─────────────────────────────────┘
```

### Экран 1: Список задач (главный)

- Фильтр: **Мои** / **Все** (задачи команды)
- Сортировка по сроку (ближайшие сверху), без срока — внизу
- Группировка: Открытые → В работе → Выполненные (свёрнуты)
- Каждая задача показывает:
  - Название
  - Исполнитель (аватар + имя)
  - Срок (если есть) — красный если просрочен
  - Статус (цветная метка)

### Экран 2: Создание задачи

- Поле: Название задачи (обязательно)
- Выбор: Исполнитель (список членов команды, по умолчанию — я)
- Выбор: Срок (date picker, опционально)
- Настройка: Напоминание (если есть срок)
  - За 1 день / В день срока / Своё время
- Кнопка: **Создать**

### Экран 3: Просмотр/редактирование задачи

- Все поля задачи
- Кнопки смены статуса: **В работу** / **Готово**
- Редактирование полей
- Кнопка: **Удалить**

### Экран 4: Управление командой (для лидера)

- Список участников
- Invite-ссылка (копировать)
- Настройка: кто может создавать задачи
- Удалить команду

---

## Секция 3: Telegram-бот

### Роль бота

Бот — это **точка входа** и **канал уведомлений**. Основная работа в Mini App.

### Команды бота

| Команда | Действие |
|---------|----------|
| `/start` | Приветствие + кнопка "Открыть TaskMini" |
| `/new` | Быстрое создание задачи (диалог в боте) |
| `/my` | Список моих задач текстом |
| `/join XXXX` | Вступить в команду по invite-коду |

### Диалог быстрого создания (`/new`)

```
Бот: Введите название задачи:
User: Подготовить список песен
Бот: Выберите команду:
     [Прославление] [Молодёжка]
User: [Прославление]
Бот: Исполнитель:
     [Я] [Выбрать другого]
User: [Я]
Бот: Срок? (или пропустить)
     [Сегодня] [Завтра] [Выбрать дату] [Пропустить]
User: [Завтра]
Бот: ✓ Задача создана.
     [Открыть в TaskMini]
```

### Уведомления от бота

**Напоминания:**
```
🔔 Напоминание: "Подготовить список песен"
Срок: завтра
[Открыть задачу]
```

**Назначение задачи:**
```
📋 Вам назначена задача: "Привезти аппаратуру"
Команда: Прославление
Срок: 22 декабря
[Открыть задачу]
```

**Задача выполнена (создателю):**
```
✓ Задача выполнена: "Привезти аппаратуру"
Исполнитель: @petrov
```

---

## Секция 4: Техническая архитектура

### Общая схема

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  Telegram App    │────▶│  Google Apps     │────▶│  Google Sheets   │
│  (Mini App UI)   │     │  Script          │     │  (База данных)   │
└──────────────────┘     └──────────────────┘     └──────────────────┘
         │                       │
         │                       ▼
         │               ┌──────────────────┐
         │               │  Telegram Bot    │
         │               │  API             │
         └───────────────┴──────────────────┘
```

### Компоненты GAS

| Файл | Назначение |
|------|------------|
| `Code.gs` | API handlers, бизнес-логика |
| `Bot.gs` | Webhook для Telegram бота |
| `Triggers.gs` | Time-based триггеры для напоминаний |
| `index.html` | Mini App интерфейс |

### API endpoints (через `google.script.run`)

| Метод | Действие |
|-------|----------|
| `getMyTeams()` | Список команд пользователя |
| `getTeamTasks(teamId)` | Задачи команды |
| `createTask(data)` | Создать задачу |
| `updateTask(taskId, data)` | Обновить задачу |
| `deleteTask(taskId)` | Удалить задачу |
| `createTeam(name)` | Создать команду |
| `joinTeam(inviteCode)` | Вступить в команду |
| `getTeamMembers(teamId)` | Участники команды |

### Telegram Bot Webhook

- Бот получает сообщения через `doPost(e)`
- Парсит команды `/start`, `/new`, `/my`, `/join`
- Отправляет ответы через Telegram Bot API

### Триггеры (напоминания)

- Time-driven trigger каждые 15 минут
- Проверяет задачи с `due_date` и `reminder_settings`
- Отправляет уведомления через Telegram Bot API
- Логирует отправленные напоминания (чтобы не дублировать)

### Аутентификация

- Telegram `initData` передаётся с каждым запросом
- Для MVP: парсим `user_id` без криптографической проверки
- Для продакшена: добавить HMAC верификацию через `BOT_TOKEN`

---

## Секция 5: Путь пользователя

### Сценарий A: Лидер создаёт команду

1. Лидер находит бота @TaskMiniBot
2. `/start` → "Добро пожаловать в TaskMini" → [Создать команду] [Присоединиться]
3. Нажимает [Создать команду]
4. Mini App открывается → форма:
   - Название команды: "Прославление"
   - Кто создаёт задачи: [Все] / [Только я]
5. Команда создана → показан invite-код: "PRSV2024"
6. Лидер копирует ссылку: `t.me/TaskMiniBot?start=PRSV2024`
7. Отправляет в чат команды

### Сценарий B: Участник присоединяется

1. Участник получает ссылку в чате
2. Переходит → бот показывает: "Вас приглашают в команду «Прославление»" [Присоединиться]
3. Нажимает → добавлен в команду
4. "Вы в команде! Откройте TaskMini" [Открыть TaskMini]

### Сценарий C: Создание первой задачи

1. Лидер открывает Mini App
2. Видит пустой список: "Пока нет задач"
3. Нажимает [+ Задача]
4. Заполняет:
   - "Подготовить список песен"
   - Исполнитель: @masha
   - Срок: воскресенье
   - Напоминание: за 1 день
5. Создаёт → задача в списке
6. @masha получает уведомление: "Вам назначена задача..."

### Сценарий D: Выполнение задачи

1. @masha открывает Mini App
2. Видит задачу в "Мои задачи"
3. Начинает работу → [В работу]
4. Статус меняется на "В работе"
5. Завершает → [Готово]
6. Создатель получает уведомление: "Задача выполнена"

---

## Секция 6: Граничные случаи и ошибки

### Пользователь не в команде

```
Mini App: "У вас пока нет команд"
[Создать команду] [Присоединиться по коду]
```

### Участник удалён из команды

- Задачи, назначенные на него → остаются, помечаются "(участник вышел)"
- При открытии Mini App → эта команда не показывается
- Задача без исполнителя → можно переназначить

### Лидер покидает команду

- Если есть другие участники → первый вступивший становится лидером
- Если лидер единственный → команда удаляется вместе с задачами

### Просроченная задача

- Срок прошёл → задача остаётся в списке
- Дата подсвечивается красным: "Просрочено: 15 дек"
- Напоминания прекращаются после срока
- Можно выполнить или удалить

### Нет интернета

```
Mini App: "Нет соединения. Проверьте интернет."
[Повторить]
```

Данные не кешируются локально (MVP) — нужен интернет.

### Invite-код неверный

```
Бот: "Команда не найдена. Проверьте код приглашения."
```

### Команда с таким названием уже есть

Разрешаем — команды идентифицируются по `team_id`, не по имени.

---

## Следующие шаги

1. Создать Telegram бота через @BotFather
2. Создать Google Sheet с тремя листами
3. Создать Google Apps Script проект
4. Реализовать MVP по этому дизайну
